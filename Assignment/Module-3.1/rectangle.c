#include<stdio.h>
int main()
{
	int l,b,area;
	printf("Enter the length of reatangle:");
    scanf("%d",&l);
	printf("Enter the breadth of the reatangle:");
	scanf("%d",&b);
	area=l*b;
	printf("the value  of %d and %d is %d",l,b,area);
	
	}
	
